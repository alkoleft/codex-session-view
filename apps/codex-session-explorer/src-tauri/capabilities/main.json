{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Окно codex-session-explorer получает только read-only IPC к session catalog и preview/tail backend.",
  "windows": ["main"],
  "permissions": [
    "default",
    "core:app:default",
    "core:event:default",
    "core:path:default",
    "core:window:default",
    "log:default",
    "opener:default"
  ]
}
