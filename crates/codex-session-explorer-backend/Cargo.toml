[package]
name = "codex-session-explorer-backend"
version = "0.1.0"
edition = "2021"

[dependencies]
codex-log = { path = "../codex-log" }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
tempfile = "3.12"
