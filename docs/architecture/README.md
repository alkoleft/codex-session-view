# Архитектура

Этот каталог содержит архитектурную документацию `codex-session-view`.

## Документы

- [Документация arc42](./arc42/architecture.md) — основное архитектурное описание продукта, логового ядра, viewer и следующего этапа с отдельным CLI.

## Область покрытия

Сейчас основной акцент документации:

- структура монорепозитория `codex-session-view` с техническими частями `codex-log` + `codex-session-explorer`;
- каноническая модель чтения и отображения логов;
- место будущего CLI для чтения логов и отправки их в БД без реализации в текущем scope.
